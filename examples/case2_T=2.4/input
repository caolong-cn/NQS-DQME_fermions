cuda
60 20 0     # nhidden  nauxillary epsilontuning_mode
2 3 0       # nonmccut  allcut Lsaving_mode(if nonmc, nonmccut=allcut=3; if mc, nonmccut+1=allcut=3) 
4096 0 0     # sampling number  N_start N_end
0.0 1.0 0       # (for td) count  t_lower
1.e-8 1.e-10 #init_epsilon min_epsilon
#epsilontuning_mode = 0,1,2 mid,quick,slow
#Lsaving_mode = 0, saving;1, not saving

